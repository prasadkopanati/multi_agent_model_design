# Session Summary — 2026-04-21 16:37

## What We Did

Full end-to-end code review and bug-fix pass on the multi-agent orchestration system scaffolding. The system orchestrates a deterministic pipeline (spec → plan → build → test → review) using Claude Code (Controller) and OpenCode/Qwen (Executor).

---

## Phase 1: Code Review

Conducted a five-axis review (correctness, readability, architecture, security, performance) across all source files. Produced `TODO.md` at the repo root categorizing 15 issues across four severity levels.

**Files reviewed:**
- `orchestrator/orchestrator.js`
- `orchestrator/retry.js`
- `orchestrator/failure.js`
- `orchestrator/promptCompiler.js`
- `agent-cli/agent-cli.js`
- `agent-cli/runners/claude.js`
- `agent-cli/runners/opencode.js`
- `tasks.json`, `package.json`, `.env.example`, prompt templates

---

## Phase 2: Critical Bugs Fixed (3 issues)

| Bug | Fix |
|-----|-----|
| Circular dependency `orchestrator.js` ↔ `retry.js` — `runStage` was `undefined` on first retry | Removed top-level import from `retry.js`; `runStage` is now injected as a parameter into `retryStage` |
| Command injection in `claude.js` — prompt content interpolated into shell string | Replaced `execSync` + template string with `spawnSync` + args array; no shell involved |
| Same command injection in `opencode.js` | Same fix as above |

---

## Phase 3: High Bugs Fixed (4 issues)

| Bug | Fix |
|-----|-----|
| Global failure counter — retry budget shared across all stages | `shouldEscalate` now filters `failure_state.history` by the current stage before comparing to `retry_limit` |
| Runners bypass compiled prompt — raw template used instead of compiled artifact | Both runners now read the `input` parameter (compiled file) as system prompt instead of re-reading the raw template |
| `workspace` not passed as `cwd` to runners | `agent-cli.js` forwards `workspace` to runners; runners pass it as `cwd` to `spawnSync` |
| No tests | Added `tests/retry.test.js`, `tests/failure.test.js`, `tests/promptCompiler.test.js` using `node:test` (no deps); `npm test` added to `package.json` |

---

## Phase 4: Medium Bugs Fixed (4 issues)

| Bug | Fix |
|-----|-----|
| All file paths relative to CWD — breaks if process started from another directory | All four orchestrator files now use `path.join(__dirname, '..', ...)` constants |
| `replace()` only substitutes first placeholder occurrence | All substitutions changed to `replaceAll()` |
| `{{DEBUGGING}}` placeholder in `failure.md` never substituted | Added `{{DEBUGGING}}` substitution to `compilePrompt`; added `failure` entry to `STAGE_SKILLS`; also added `{{FAILURE}}` to `failure.md` (was missing — failure logs were never injected into the analysis prompt) |
| Pipeline stages run with empty context — artifacts not threaded forward | `runPipeline` now accumulates context after each stage by reading `artifacts/output/${stage}.json`; `promptCompiler` gained `{{SPEC}}`, `{{BUILD}}`, `{{TEST}}`, `{{ANALYSIS}}` substitutions alongside the existing `{{PLAN}}` |

---

## Phase 5: Low Bugs Fixed (4 issues)

| Bug | Fix |
|-----|-----|
| Two `Date.now()` calls in `failure.js` could produce different timestamps | Already resolved during the Medium pass (`const ts = Date.now()` reused for both uses) |
| `retryStage` passed raw error string instead of structured failure-analysis output | Added `analyzeFailure` in `retry.js`: runs `runStage("failure", ...)`, reads `artifacts/output/failure.json`, returns structured JSON. Analysis is best-effort — if it throws, retry proceeds with the raw error. `promptCompiler` exposes result via `{{ANALYSIS}}` |
| Arg parser in `agent-cli.js` silently misparsed flags without values | Replaced hand-rolled loop with `node:util`'s `parseArgs`; typed options throw on bad input |
| `npm start` did nothing — `orchestrator.js` only exported, never invoked | Added `require.main === module` guard with `parseArgs` for `--workspace`; `package.json` start script now passes `--workspace .` |

---

## Final Test State

```
15 tests, 15 passing, 0 failing
```

Test files:
- `tests/failure.test.js` — captureFailure object shape and disk write
- `tests/retry.test.js` — shouldEscalate per-stage logic (5 cases) + analyzeFailure fallback contract
- `tests/promptCompiler.test.js` — placeholder substitution, DEBUGGING injection, replaceAll, unknown stage

---

## Files Changed

| File | Changes |
|------|---------|
| `orchestrator/orchestrator.js` | Absolute paths, removed lazy require, artifact threading in pipeline, `require.main` entry point |
| `orchestrator/retry.js` | Absolute paths, per-stage escalation, `analyzeFailure` helper, `runStage` param injection |
| `orchestrator/failure.js` | Absolute paths, single `ts` capture |
| `orchestrator/promptCompiler.js` | Absolute paths, `replaceAll`, `failure` stage skills, `{{DEBUGGING}}` + `{{ANALYSIS}}` + `{{SPEC}}` + `{{BUILD}}` + `{{TEST}}` substitutions |
| `agent-cli/agent-cli.js` | `parseArgs` from `node:util`, workspace forwarded to runners |
| `agent-cli/runners/claude.js` | `spawnSync` (no shell), uses compiled `input` as system prompt, `workspace` as `cwd` |
| `agent-cli/runners/opencode.js` | Same as claude.js |
| `prompts/failure.md` | Added `{{FAILURE}}` placeholder |
| `package.json` | Added `test` script, updated `start` script |
| `TODO.md` | Created; all 15 issues now marked `[x]` |
| `tests/retry.test.js` | Created |
| `tests/failure.test.js` | Created |
| `tests/promptCompiler.test.js` | Created |
